---
title: "第二章 Linux 系统的软件开发工具"
author: "张兆飞"
date: 2026-04-05
lang: zh-CN
mainfont: "Times New Roman"
CJKmainfont: "Songti SC"
monofont: "Roboto Mono"
geometry:
  - top=2cm
  - bottom=2cm
  - left=2cm
  - right=2cm
---


# 第五周 习题

## 2-1. 在 vi 中，以下各命令的功能为

```
G             将光标移到___(1)___
5G            将光标移到___(2)___
w             将光标移到___(3)___
B             将光标移到___(4)___
___(5)___     将光标移到本行首非空字符
$             将光标移到___(6)___
___(7)___     将光标向文件首方向翻一屏
o             在___(8)___插入内容
i             在___(9)___插入内容
在插入模式中, 必须用___(10)___键退回到命令模式
J             将下一行___(11)___
yy            将___(12)___存入无名缓冲区
___(13)___    交换当前字符和下一字符的组合命令
___(14)___    交换当前行和下一行的组合命令

```

(1) 文件的最后一行 (如果该行为空行, 则光标停留在行首; 否则停留在第一个非空字符上)

(2) 第 5 行 (光标停留的列与上一条类似; 如果该行不存在, 则停留在最后一行)

(3) 下一个 `word` 的第一个字符 (`word` 表示纯字母, 数字和下划线)

(4) 上一个 `WORD` 的第一个字符 (`WORD` 表示以空白字符分隔的字符串)

(5) `^`

(6) 本行的最后一个字符

(7) `Ctrl-u` (半屏) 或 `Ctrl-b` (全屏)

(8) 当前行的下一行

(9) 当前行的当前位置 (当前光标前)

(10) `Esc`

(11) 合并到当前行末尾, 用一个空格分隔 (如果下一行为空行, 则直接删除下一行, 不添加空格)

(12) 当前行

(13) `xp`

(14) `ddp`


## 2-2. 完成以下用 vi 编辑文件的过程

```
2-2-1. 文件内容为
    Can I
    barrow the baat
    twa for days?
执行命令 GdwwP 后, 文件内容为
    Can I
    ___(1)___
    ___(2)___
再执行命令 1G/a/<CR>nron.nn. 后, 文件内容为
    Can I
    ___(3)___
    ___(4)___

```

(1) `barrow the baat`

(2) `for twa days?`

(3) `borrow the boat`

(4) `for two days?`

```
2-2-2. 文件内容为 (以下每一答案为一个字符)
    may I keep it
    ?Excuse me,
    Eofr a along period
执行命令
    2G___(1)___G___(2)___p___(3)___2w___(4)___
后, 文件内容为
    may I keep it
    Excuse me,
    Eofr a long period?
再执行命令
    ___(5)___xx___(6)___1G___(7)______(8)___p
后, 文件内容为
    Excuse me,
    may I keep it
    for a long period?

```

(1) `x`

(2) `$`

(3) `^`

(4) `x`

(5) `^`

(6) `p`

(7) `d`

(8) `d`

